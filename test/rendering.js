const { expect } = require('chai');

describe('Render', function() {
  it('should render a blank game when neither player has made a move', function() {
    expect(false).to.be.true;
  });

  it('should render whose turn it is', function() {
    expect(false).to.be.true;
  });

  it('should render when a move is made', function() {
    expect(false).to.be.true;
  });
});